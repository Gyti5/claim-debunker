services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: claim-debunker-app
    environment:
      COMPOSITION_API_KEY: ci-test-key
    ports:
      - "8080:8080"
