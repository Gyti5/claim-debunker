env: local

server:
  host: 0.0.0.0
  port: 8080

composition:
  max_upload_bytes: 10485760

openai:
  api_key: ""
  model: gpt-5.5
  base_url: https://api.openai.com/v1/responses
  timeout: 45s

security:
  composition_api_key: replace-with-random-secret
  composition_rate_limit_per_minute: 60
